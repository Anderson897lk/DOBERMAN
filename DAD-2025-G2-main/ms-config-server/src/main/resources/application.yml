server:
  port: 7071

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alexjhona/ProyectoDespliegueDeSoftware2026.git
          search-paths:
            - DAD-2025-G2-main/config-data
          default-label: main
          clone-on-start: true